// Template-literal variant: the specifier appears only inside a `template` string.
const docs = `Federated imports look like: import('remoteApp/App')`;
console.log('trap_template.js loaded — if you see this in the browser, the template-literal variant did NOT reproduce.', docs);
