// String-literal variant: the specifier appears only inside a string value.
const docs = "Federated imports look like: import('remoteApp/App')";
console.log('trap_string.js loaded — if you see this in the browser, the string-literal variant did NOT reproduce.', docs);
